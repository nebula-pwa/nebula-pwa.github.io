import React from 'react';
import '../styles/HomeBanner.css'; // Import the updated CSS for the banner

const Banner = () => {
  return (
    <div className="homepage-banner">
      <h1 className="animate-text">Welcome to Nebula PWA Info Hub</h1>
    </div>
  );
};

export default Banner;
